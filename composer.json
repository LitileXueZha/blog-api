{
    "name": "litilexuezha/blog-api",
    "description": "给博客提供接口服务",
    "type": "project",
    "require": {
        "monolog/monolog": "^1.24"
    },
    "require-dev": {
        "phpunit/phpunit": "^8.1"
    },
    "scripts": {
        "start": "php -S 127.0.0.1:9100 -t public",
        "test": "phpunit --testdox --coverage-text",
        "report": "phpunit --coverage-html coverage --coverage-clover coverage/report.xml"
    }
}
